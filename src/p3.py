#!/usr/bin/python
#!encoding: UTF-8

a = float(raw_input('Valor de a: '))
b = float(raw_input('Valor de b: '))

x = -b / a #Habría que hacer una condición para que a sea distintqa de 0 

pint 'Solucion: ', x